version: 0.2

phases:
    install:
       commands:
        - yum install -y yum-utils
        - yum install -y which
        - yum-config-manager --enable epel
        - yum install -y zip p7zip
        - yum install -y golang gox gofmt
        - ./build/linux/src/FanOutConfigurator/FanOutConfigurator ./config.yaml
    build:
      commands:
    post_build:
      commands:
artifacts:
  files:
    - '**/*'
