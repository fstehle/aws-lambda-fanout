version: 0.2

phases:
    install:
       commands:
        - ls -al
        - yum install -y zip
        - chmod 770 FanOutConfigurator
        - chmod 770 fanout
        - ./FanOutConfigurator ./config.yaml
    build:
      commands:
    post_build:
      commands:
artifacts:
  files:
    - '**/*'
