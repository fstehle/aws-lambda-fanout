version: 0.2

phases:
    install:
       commands:
        - chmod 770 FanOutConfigurator
        - ./FanOutConfigurator config.yaml
    build:
      commands:
    post_build:
      commands:
artifacts:
  files:
    - '**/*'
